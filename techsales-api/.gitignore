node_modules/
dist/
*.log
npm-debug.log*

# Secrets — never commit. `.env` holds live API keys; the template is .env.example.
.env*
!.env.example
SSHDetails.md
TechSales.config
*.pem
*.key
id_rsa*
.claude/settings.local.json

# Runtime / lookup JSON stores — operator-owned, NOT checked in.
# Sample data IS checked in (it's the bootstrap seed).
data/runtime/*
data/lookup/*
!data/runtime/.gitkeep
!data/lookup/.gitkeep

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
